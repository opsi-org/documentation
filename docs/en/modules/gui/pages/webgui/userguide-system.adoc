////
; Copyright (c) uib GmbH (uib.de)
; This documentation is owned by uib
; and published under the german creative commons by-sa license
; see:
; https://creativecommons.org/licenses/by-sa/3.0/de/
; https://creativecommons.org/licenses/by-sa/3.0/de/legalcode
; english:
; https://creativecommons.org/licenses/by-sa/3.0/
; https://creativecommons.org/licenses/by-sa/3.0/legalcode
;
; credits: https://www.opsi.org/credits/
////

:Author:    uib GmbH
:Email:     info@uib.de
:Date:      21.07.2026
:Revision:  4.3
:toclevels: 6
:doctype:   book
:icons:     font
:xrefstyle: full



[[opsi-manual-opsiwebgui-system]]
= Admin

The _Admin_ section (accessible from the left navigation bar) provides access to system administration features: a remote terminal, diagnostic tools, and maintenance functions.

[[opsi-manual-opsiwebgui-system-terminal]]
== Terminal

The terminal provides access to a server console via the xref:server:components/opsiconfd.adoc#server-components-opsiconfd-admin-page-messagebus[OPSI-Messagebus].
The OPSI config server is entered here by default.
Press *[ Connect ]* to establish a connection to the home directory (`/var/lib/opsiconfd/home/`) as user `opsiconfd`.

.OPSI-WebGUI: Admin terminal
image::gui:webgui/opsi-webgui-admin-terminal.png["OPSI-WebGUI: Admin terminal"]

Once connected, the terminal behaves like a standard shell session on the opsiconfd host. You can use it to inspect logs, run OPSI CLI commands, and perform manual maintenance tasks.
The *[ Disconnect ]* button closes the session cleanly. 
Alternatively, typing `exit` in the terminal ends the session, after which the terminal disconnects automatically.


You can customize the terminal session via the _Settings_ button:

* *Terminal ID*: A unique identifier for the terminal session. The default value is auto-generated. Change it only if you need to reconnect to a specific session.
* *Terminal Channel*: The messagebus channel to connect to. By default this is the config server; enter a depot server's service channel to open a terminal there instead.

NOTE: If the terminal is unavailable, see the troubleshooting entry xref:gui:webgui/userguide-troubleshooting.adoc#opsi-manual-opsiwebgui-troubleshooting-terminal-not-available[Terminal Not Available].


[[opsi-manual-opsiwebgui-system-healthcheck]]
== Diagnostics

Navigate to _Admin_ / _Diagnostics_ to open the combined diagnostics view.
The page is organized into three tabs: _Health Check_, _Modules_, and _System Info_.

[[opsi-manual-opsiwebgui-system-healthcheck-tab]]
=== Health Check

The Health Check tab runs automated checks of your OPSI components and displays the results.
You can find out more at xref:server:components/monitoring.adoc#server-components-opsiconfd-health-check[opsiconfd health check].

.OPSI-WebGUI: Health Check
image::gui:webgui/opsi-webgui-admin-diagnostics-healthcheck.png["OPSI-WebGUI: Health Check"]

Each check result is shown as a row with a status badge:

* *OK* (green): The component is functioning normally.
* *Warning* (yellow): Something requires attention but is not critical.
* *Error* (red): A problem that should be resolved promptly.

Use the status filter buttons at the top to show only OK, warning, or error results.
Click any row to expand it and read the detailed description and recommended action.

TIP: The _Dashboard_ also shows a summary of the health check state. Click the health check badge, or the warning/error indicator in the Top Bar, to open this page.

[[opsi-manual-opsiwebgui-system-modules-tab]]
=== Modules

The Modules tab lists all OPSI extensions (licensed modules) available on the server.

.OPSI-WebGUI: Modules
image::gui:webgui/opsi-webgui-admin-diagnostics-modules.png["OPSI-WebGUI: Modules"]

[[opsi-manual-opsiwebgui-system-sysinfo-tab]]
=== System Info

The System Info tab displays technical information about the server running opsiconfd.

.OPSI-WebGUI: System Info
image::gui:webgui/opsi-webgui-admin-diagnostics-system-info.png["OPSI-WebGUI: System Info"]

[[opsi-manual-opsiwebgui-system-admin]]
== Maintenance

Navigate to _Admin_ / _Maintenance_ to access maintenance tasks.

.OPSI-WebGUI: Maintenance
image::gui:webgui/opsi-webgui-admin-maintenance.png["OPSI-WebGUI: Maintenance"]

The Maintenance page is organized into four functional areas:

[[opsi-manual-opsiwebgui-system-admin-blockedclients]]
=== Blocked Clients

Displays clients that have been temporarily blocked by opsiconfd, typically because of repeated failed authentication attempts.

* *View*: The blocked clients list shows the client ID.
* *Unblock individual*: Click the unblock button next to a client to remove the block immediately.
* *Unblock all*: A single button removes all blocks at once.

[[opsi-manual-opsiwebgui-system-admin-lockedproducts]]
=== Locked Products

Displays products that are currently locked by the server (usually because an installation or an action is in progress).

* *View*: Each row shows the product ID and the depot that holds the lock.
* *Unlock*: Use the unlock button to release a lock if an operation was interrupted unexpectedly.

[[opsi-manual-opsiwebgui-system-admin-maintenancemode]]
=== Maintenance Mode

The _Application State_ card shows whether the server is running normally or in maintenance mode (see section xref:server:components/opsiconfd.adoc#server-components-opsiconfd-admin-page-maintenance[Maintenance] in the opsiconfd documentation).

* *Maintenance*: Switch to maintenance mode.
* *Normal*: Switch back to normal operation.

[[opsi-manual-opsiwebgui-system-admin-backup]]
=== Backup and Restore

The backup section lets you create and restore server configuration backups directly from the OPSI-WebGUI.

*Creating a backup*:

. Select which options to use: _Maintenance mode_ (put the server into maintenance mode while the backup runs), _Configuration files_, and/or _Redis data_.
. Optionally enter an encryption password. If provided, the backup file is encrypted and the password will be required to restore it.
. Click _Create Backup_.
. The backup file is downloaded by your browser.

*Restoring a backup*:

. Click _Upload Backup_ and select a previously created backup file.
. If the backup was encrypted, enter the password.
. Click _Restore_.
. opsiconfd will apply the backup content and may restart services as needed.

For detailed information on backup and restore, refer to the chapter xref:server:components/backup.adoc[Backup of the OPSI Server].